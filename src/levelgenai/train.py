"""Phase 2 (not yet implemented): training loop over a data/snapshots/dataset_v{N}.jsonl
snapshot (see manifest.compile_snapshot), stratified train/val split by difficulty,
next-token cross-entropy with teacher forcing, early stopping on val loss.
"""

raise NotImplementedError("Phase 2 — see module docstring and the plan.")
