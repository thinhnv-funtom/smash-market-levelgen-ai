"""Phase 2/4 (not yet implemented): sampling entry point the Unity Editor tool
shells out to. Given a requested difficulty/size profile, samples N candidate
token sequences (temperature/top-k), decodes each through tokenizer.py back into
level JSON, and runs validators.py before returning anything to Unity.
"""

raise NotImplementedError("Phase 2/4 — see module docstring and the plan.")
